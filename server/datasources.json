{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongo": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "${DB_URL}",
    "database": "${DB_NAME}",
    "password": "${DB_PASS}",
    "name": "mongodb",
    "user": "${DB_USER}",
    "connector": "mongodb"
  }
}
